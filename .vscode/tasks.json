{
  "tasks": [
    {
      "label": "Compile and Run c++ file",
      "type": "shell",
      "command": "g++ -Wall ${file} -o ${fileDirname}${pathSeparator}${fileBasenameNoExtension}.out && ${fileDirname}${pathSeparator}${fileBasenameNoExtension}.out",
      "problemMatcher": []
    }
  ],
  "version": "2.0.0"
}