#include <iostream>
#include <fstream>
#include <string>

using std::string;

#ifndef ONLINE_JUDGE
std::ifstream cin("input.txt");
std::ofstream cout("output.txt");
#endif

#define TRUE "TRUE"
#define FALSE "FALSE"

int main()
{
    std::string cubes;

    while (cin >> cubes)
    {

        for (size_t i = 0; i < 6; i++)
        {
            for (size_t i = 0; i < 6; i++)
            {
                for (size_t i = 0; i < 6; i++)
                {
                }
            }
        }
    }
    return 0;
}